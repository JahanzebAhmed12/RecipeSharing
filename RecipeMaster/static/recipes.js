document.getElementById('searchForm').onsubmit = function(e) {
    e.preventDefault();
    // Fetch and display recipes based on search/filter (to be implemented)
    document.getElementById('recipeList').innerHTML = '<p>Recipe search/filter coming soon.</p>';
}; 